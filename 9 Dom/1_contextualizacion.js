// ...................................DOM.....................................
    
    // 1. El DOM es la interface entre JS y el navegador (Mas expecificamente el HTML)
        // a. Podemos crear y modificar estilos, clases y aributos y responder a los eventos.
        // b. El DOM se genera a partir de cualquier elemento HTML. 
        // c. DOM es un API muy compleja (interface de aplicación de aplicaciones).
        // d. El DOM contienen propiedades y metodos que podemos usar para interactuar entre JS y HTML