// ASYNC

    // 1. RECURSOS..............................
        
        // Sincronico, es cuando la ejecución del codigo se da linea por linea en el orden que está escrita nuestra aplicación, esperando que la linea anterior termine de ejecutarse
            
            // - Se pueden tener problemas cuando una linea tarda demasiado en ejecutarse. 

        // Asincronico (No bloqueante) , es cuando la ejecución de una parte del codigo se ejecuta en segundo plano sin parar la ejecución del programa.

            // - Se pospone una acción en el futuro para que el codigo no se bloquee
            // - Los artibutos de las etiquetas se ejecutan de manera asincronica, por ejemplo al cargar una imagen, para que no se deba de esperar todo el tiempo de carga (en este caso cuando un elemento termina de cargarse ejecuta el evento load)

